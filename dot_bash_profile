# .bash_profile

if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi

export PATH="${HOME}/.local/bin:${PATH}"

export PYENV_ROOT="$HOME/.pyenv"

if command -v pyenv 1>/dev/null 2>&1; then
    eval "$(pyenv init -)"
fi
